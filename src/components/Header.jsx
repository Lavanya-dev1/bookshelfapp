// Header component — displays the app title at the top of every page
function Header() 
{
    return(
        <header>
            BookShelfApp
        </header>
    );
}
// Export the Header component so it can be imported in App.jsx
export default Header;